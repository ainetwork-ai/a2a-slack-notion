legacy-peer-deps=true
# Hoist all transitive deps to the root so Turbopack/webpack module resolution
# works inside `slack/` without needing explicit transpilePackages entries
# (pnpm's default symlinked layout confuses Next 16's Turbopack on Vercel).
shamefully-hoist=true
