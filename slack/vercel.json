{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd .. && ls -la node_modules slack/node_modules 2>&1 | head -5 && rm -rfv node_modules slack/node_modules 2>&1 | head -5 && ls node_modules slack/node_modules 2>&1 | head -5 && pnpm install --frozen-lockfile --reporter=append-only 2>&1 | tail -20",
  "buildCommand": "cd .. && pnpm --filter slack-a2a build",
  "crons": [
    {
      "path": "/api/cron/cleanup-share-links",
      "schedule": "0 3 * * *"
    },
    {
      "path": "/api/cron/compact-page-snapshots",
      "schedule": "0 4 * * 0"
    }
  ]
}
